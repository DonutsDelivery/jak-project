;;-*-Lisp-*-
(in-package goal)

;; SCAFFOLDING: jakx-init top-level is undecompiled PS2 code — original calls
;; (update *setting-control*) and similar setup that requires *setting-control*,
;; *entity-pool*, etc. to already be initialized. Those happen via main.gc's
;; display-loop, not at GAME.CGO load time. Empty top-level here.
