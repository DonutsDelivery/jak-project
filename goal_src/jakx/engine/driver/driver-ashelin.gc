;;-*-Lisp-*-
(in-package goal)


(deftype driver-ashelin (driver)
  ()
  )


;; SCAFFOLDING: defskelgroup stub (art group in level DGO, not available at compile time)
(define skel-driver-ashelin (the-as skeleton-group #f))
