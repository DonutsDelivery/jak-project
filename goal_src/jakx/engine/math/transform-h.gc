;;-*-Lisp-*-
(in-package goal)

;; name: transform-h.gc
;; name in dgo: transform-h
;; dgos: GAME

;; DECOMP BEGINS

(deftype transformq (structure)
  ((trans  vector  :inline)
   (rot    vector  :inline)
   (scale  vector  :inline)
   )
  )


(deftype trsq (basic)
  ((trans  vector  :inline)
   (rot    vector  :inline)
   (scale  vector  :inline)
   )
  )
