;; Jak X KRASTRN.DGO — Kras City training/tutorial level.
;; Game-task: kras-training (task=7); first-tutorial sequence.
;; krastrn-vis is bundled inside krastrn.go (single-file extraction);
;; the loader looks up visname 'krastrn-vis from this object.
;; Code objects (net-training, net-training-obs, wcar-drone) still deferred
;; until ported to OpenGOAL. Asset binaries below are pure data, not code,
;; so they are safe to include and are required for visible geometry
;; (per .jakx_watch/k3_full_dgo_audit.md).
("KRASTRN.DGO"
 ("tpage-1775.go"
  "tpage-2862.go"
  "training-wall-ag.go"
  "training-marker-ag.go"
  "start-light-ag.go"
  "deathrace-drone-ag.go"
  "krastrn.go"
  ))
